schema_version: 1
name: rpent-libero-smoke
benchmark: libero
cases_file: rpent-libero.cases.example.jsonl
participants: [rpent_libero]
protocol:
  track: native_system
  phase: evaluation
  memory_policy: frozen
  scoring: benchmark
  budget:
    max_control_steps: 500
    episode_timeout_s: 1200
execution:
  mode: debug
  max_jobs: 1
  max_infrastructure_retries: 0
  job_timeout_s: 1500
artifacts:
  video: all
  trace: actions
