# AGENTS.md

## 项目目标

`eva_agentic` 是外置的机器人智能体评测代码库，不是新的机器人框架。
它固定实验输入、启动框架原生评测命令、保存证据并汇总结果。它不重写框架的控制循环、服务拓扑或模型调用。
优先实现小规模、可追溯的运行闭环，再扩展批量评测和更多框架。

## 代码边界

- `third_party/` 存放干净的上游源码，并按 `benchmarks/`、`frameworks/`、`models/` 分类。它们按外部依赖对待，不默认纳入重构、格式化或依赖升级。
- 同一基准只保留一份规范源码，例如 `third_party/benchmarks/libero/`。框架通过该共享版本运行；框架所需的微小差异放到 `patches/<framework>/<benchmark>/`，并记录适用的基准提交、原因和应用方式，不复制完整基准。
- 接入逻辑优先放在评测侧。必须修改上游时，保持改动最小，并记录补丁、原因和对应版本。
- 不因第三方依赖名称相同就合并源码或软件环境；模型和仿真资产仅在版本兼容时共享。
- 每个框架保留自己的 conda、uv、容器和原生命令。框架内部启动的 planner、VLA、SAM、视觉服务和模拟器由框架负责管理。

## 架构约束

- 主控保持轻量，不直接导入框架、模拟器或模型依赖。相关代码只通过文件和一个原生框架进程通信。
- 框架适配器负责原生参数映射、单条启动命令、资源请求、产物定位和结果解析。运行器负责 conda、uv 或指定解释器的进程执行。
- 基准适配器负责任务实例、初始化要求和评分协议，不把这些逻辑写死在框架适配器或调度器中。
- 统一整次试验的输入输出，不强迫框架共用内部控制循环、内部服务编排或 `act()` 接口。
- 框架差异优先放在适配器和配置中，不在通用执行器里持续增加框架专属分支。
- 版本库保存第三方来源清单和可移植的框架声明；项目根目录的 `.venv/` 是评测器环境，`.envs/eva-*/` 是框架或模型的本机环境。profile 保存解释器、模型、资产、GPU 清单和密钥引用，不提交；证据中只记录环境变量名和脱敏值。

## 评测约束

- 统一参数必须具有明确语义。不支持或无法确认生效的条件必须显式报告；影响公平性时，不得标为可直接比较。
- 固定并记录任务实例、初始化条件、预算和记忆策略；不能只记录一个随机种子。
- 记录代码版本及本地修改、实际命令、工作目录、脱敏配置和原始输出。正式运行不得隐式更新代码或依赖。
- 进程正常退出不等于任务成功。区分任务失败、超时、基础设施故障和无效结果；缺失指标不填成零。
- 每个结果必须记录原生任务成功信号的来源。缺失、冲突或无法解析的原生信号标为无效结果。
- 保留所有尝试。区分框架内部恢复与平台重新调度，不用重跑挑选成功结果，不覆盖历史产物。
- 探索或学习与正式测试分离，明确技能和记忆的来源、冻结及继承规则。

## 开发与验证

- 修改前先阅读相关代码和原生文档，不假定框架已跑通，也不把文档示例当作本机验证结果。
- 新接口优先用假执行器或样例产物测试；真实接入先完成最小试验，再扩大任务规模。
- 测试参数映射、结果解析及失败路径。未实际执行的测试或实验明确标注“未验证”。
- 优先完成当前任务所需的最小改动，不提前搭建复杂调度平台或大范围重构。
- 密钥、本机绝对路径、环境目录、大型资产和运行产物不提交版本库。
- 交付时说明修改内容、验证方式、验证结果和剩余限制，不声称未完成的复现或公平性检查已通过。
